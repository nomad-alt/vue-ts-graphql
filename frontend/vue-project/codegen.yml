overwrite: true
schema: "http://localhost:8000/graphql" # replace with the URL of your GraphQL server
documents: "src/**/*.graphql"
generates:
  src/generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-vue-apollo"
